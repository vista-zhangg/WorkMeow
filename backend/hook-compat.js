'use strict';

// Hook filenames installed by earlier releases. The installer removes these
// entries while preserving every hook owned by other tools.
module.exports = Object.freeze(['octopus-hook.js', 'llmpet-hook.js']);
